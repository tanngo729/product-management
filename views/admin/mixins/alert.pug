mixin alert-success(time)
  - if (messages.success)
    .message.info
      .alert.alert-success(show-alert, data-time=time) #{messages.success}

mixin alert-error(time)
  - if (messages.error)
    .message.info
      .alert.alert-danger(show-alert, data-time=time) #{messages.error}


